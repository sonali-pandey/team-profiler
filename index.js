const Team = require ('./lib/Team');

console.log(`
            ===================================
            Team Profiler: Adding Team Manager
            ===================================
`);

new Team().AddTeam('Team Manager');